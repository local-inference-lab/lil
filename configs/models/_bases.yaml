# SPDX-License-Identifier: Apache-2.0
# SPDX-FileCopyrightText: Copyright contributors to the lil project

# Model families shared by several repositories. A manifest names one family;
# a family may name a parent. Mappings merge, and scalars, lists, or explicit
# nulls in the manifest replace the family value.

schema_version: 1

families:
  glm:
    serving:
      reasoning_parser: glm45
      tool_call_parser: glm47
    kernels:
      attention: B12X
      mamba_cache_mode: align
    environment:
      VLLM_SSM_CONV_STATE_LAYOUT: DS
